### Общие предусловия

*   **Host:** `https://qa-internship.avito.com`
    
*   **Изоляция данных:** Во избежание конфликтов (`409 Conflict`), для каждого тестового прогона `username` и `password` генерируются динамически.
    
*   **Авторизация:** Для доступа к защищенным эндпоинтам предварительно выполняется вызов `/api/1/register` и `/api/1/autorize`. Полученный `accessToken` передается в заголовке `Authorization: Bearer <token>`.
    
*   **Очистка:** После выполнения тестов созданные объявления удаляются через метод `DELETE /api/2/item/{id}` для поддержания чистоты стенда.
    

### 1\. Позитивные сценарии 

| ID | Сценарий | Шаги воспроизведения | Ожидаемый результат |
| ------- | ------- | ------- | ------- |
| 1 | E2E: Жизненный цикл объявления | 1. Зарегистрироваться (POST /register).2. Авторизоваться (POST /autorize).3. Создать объявление (POST /item).4. Получить его (GET /item/{id}).5. Удалить (DELETE /item/{id}).6. Запросить удаленное повторно. | Шаги 1-4: Статусы 200 OK/201 Created. Данные на шаге 4 совпадают с отправленными. Шаг 5: Объявление удалено. Шаг 6: Статус 404 Not Found. |
| 2 | Получение всех объявлений продавца | 1. Создать объявление А.2. Создать объявление Б для того же sellerID.3. Запросить GET /api/1/{sellerID}/item. | Статус 200 OK. В теле ответа возвращается массив, содержащий объекты объявлений А и Б. |
| 3 | Сравнение статистики v1 и v2 | 1. Создать объявление со статистикой.2. Вызвать GET /api/1/statistic/{id}.3. Вызвать GET /api/2/statistic/{id}. | Статус 200 OK для обеих ручек. Возвращаются корректные счетчики. |

### 2\. Негативные сценарии

| ID | Сценарий | Шаги воспроизведения | Ожидаемый результат |
| ------- | ------- | ------- | ------- |
| 4 | Доступ без токена авторизации | 1. Отправить запрос к POST /api/1/item с валидным телом, не передавая заголовок Authorization. | Статус 401 Unauthorized. Запись в БД не создается. |
| 5 | Подмена sellerID при создании | 1. Авторизоваться под Пользователем А.2. Передать в теле POST /api/1/item чужой sellerID (Пользователя Б). | Статус 400 Bad Request или 403 Forbidden. Объявление не создается. |
| 6 | Удаление чужого объявления | 1. Создать объявление Пользователем А.2. Авторизоваться под Пользователем Б.3. Попытаться удалить объявление через DELETE /api/2/item/{id}. | Статус 403 Forbidden. Объявление остается доступным для Пользователя А. |
| 7 | Запрос несуществующего ресурса | 1. Отправить GET /api/1/item/11111111-2222-3333-4444-555555555555 (несуществующий UUID). | Статус 404 Not Found. |

### 3\. Граничные значения и Валидация типов 

| ID | Сценарий | Входные данные (Тело POST /api/1/item) | Ожидаемый результат |
| ------- | ------- | ------- | ------- |
| 8 | Невалидная цена (Ниже границы) | "price": -1"price": 0 | Статус 400 Bad Request. Ошибка валидации поля price. |
| 9 | Превышение типа данных (Цена) | "price": 9999999999999999999 | Статус 400 Bad Request. Система не должна падать с 500 Internal Server Error. |
| 10 | Отрицательные счетчики статистики | "contacts": -5, "likes": -1, "viewCount": -10 | Статус 400 Bad Request. Счетчики не могут быть отрицательными. |
| 11 | Нарушение типов данных (String вместо Int) | "price": "пять тысяч", "sellerID": "admin" | Статус 400 Bad Request. Ошибка типизации JSON. |
| 12 | Отсутствие обязательных полей | Отправить JSON без поля name. | Статус 400 Bad Request. Ошибка валидации. |
| 13 | Сверхдлинное значение строки | В поле name передать строку из 10 000 символов. | Статус 400 Bad Request (ограничение длины) или успешное сохранение без падения сервера с 500 ошибкой. |
| 14 | Отсутствие опциональных блоков | Передать JSON без блока statistics вообще. | Статус 200/201. Объявление создается, статистика по умолчанию устанавливается в нули. |
| 15 | XSS-инъекция | В поле name передать  тег: <script>alert('XSS')</script>. | Статус 200/201. При GET-запросе строка возвращается как обычный текст, скрипт не выполняется. |