# Отчет о найденных дефектах 

**Окружение для всех дефектов:** `https://qa-internship.avito.com`

| ID | Описание дефекта | Серьезность | Шаги воспроизведения | Фактический результат | Ожидаемый результат |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **Возможность удалить чужое объявление**<br>Отсутствует проверка принадлежности ресурса. | Критический | 1. Создать объявление Пользователем А (получить `id`).<br>2. Авторизоваться Пользователем Б.<br>3. Вызвать `DELETE /api/2/item/{id}` с токеном Пользователя Б. | Статус `200 OK`. Объявление Пользователя А удаляется из БД. | Статус `403 Forbidden` или `401 Unauthorized`. Объявление не удаляется. |
| **2** | **Отсутствие валидации отрицательной цены** | Высокий| 1. Отправить POST `/api/1/item`.<br>2. В теле передать `"price": -1`. | Статус `200 OK`. Объявление сохраняется с отрицательной стоимостью. | Статус `400 Bad Request`. Ошибка валидации поля price. |
| **3** | **Отсутствие валидации отрицательной статистики** | Средний| 1. Отправить POST `/api/1/item`.<br>2. В блоке `statistics` передать отрицательные значения (например, `"contacts": -5`). | Статус `200 OK`. Сервер принимает и сохраняет отрицательные счетчики. | Статус `400 Bad Request`. Счетчики не могут быть меньше нуля. |
| **4** | **Сбой при отсутствии опционального блока statistics** | Средний| 1. Отправить POST `/api/1/item`.<br>2. Не передавать поле `statistics` в теле запроса. | Статус `400 Bad Request`. | Статус `200 OK` (или `201`). Блок должен быть опциональным, счетчики по умолчанию = 0. |
| **5** | **Архитектурный баг: GET по ID возвращает массив** | Средний | 1. Создать объявление.<br>2. Отправить запрос `GET /api/1/item/{id}`. | Тело ответа имеет структуру массива: `[ { "id": "...", ... } ]`. | Тело ответа должно быть объектом: `{ "id": "...", ... }`. |